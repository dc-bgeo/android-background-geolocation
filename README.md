# BGeo Android SDK

An open Kotlin facade over the closed-source BGeo background-geolocation engine.
The engine AAR (`dev.bgeo:bgeo-android`) is vendored as a local Maven repo under
`libs/` until it's published to Maven Central.

## Running tests

```
./gradlew :sdk:test
```
